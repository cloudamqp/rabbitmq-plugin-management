Allows you to enable and disable plugins from the management interface.
Build it like any other plugin.

HTTP API:
GET /api/plugins list available plugins
PUT /api/plugins/:plugin-name enables a plugin
DELETE /api/plugins/:plugin-name disabled a plugin

Based on the Rabbit Shovel Management plugin

